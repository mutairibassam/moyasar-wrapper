{
  "name": "moyasar-wrapper",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit -p packages/shared && bunx tsc --noEmit -p packages/db && bunx tsc --noEmit -p apps/api"
  },
  "devDependencies": {
    "bun-types": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
